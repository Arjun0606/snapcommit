# Launch-day Twitter thread

10-12 tweets. Post on launch day at 9 AM PT (right after HN goes live).

Add the demo video as the first tweet's media. Use real screenshots.

---

**1/**
Every new AI session: re-explain your project, re-state preferences, re-paste context.

91 hours a year, per a 500-Reddit-complaints analysis.

I built Snapcommit so this doesn't happen anymore.

[VIDEO: 90s demo]

---

**2/**
One MCP server. Works across Claude Code, Cursor, VS Code, Claude Desktop, Cline, Windsurf, Codex CLI, Gemini CLI, Zed, Continue — 30+ clients.

Install once. Your AI tools remember now.

`npx @snapcommit/install`

---

**3/**
The trust thing matters: your memories live in a file YOU own.

→ Local SQLite by default
→ Cross-device? Point at your iCloud / Dropbox / OneDrive / Google Drive folder. Your cloud handles sync. We don't.
→ Team? Shared cloud folder. We don't build that either.

We're the wire, not the warehouse.

---

**4/**
The differentiator I haven't seen anywhere:

Snapcommit captures REJECTIONS.

Not just "we decided to use X." Also: "we tried Y, rejected it because Z."

So your AI doesn't keep suggesting the approach you already shot down.

---

**5/**
The competitive landscape is honest:

OpenMemory claims local-first → ships to Mem0's cloud (Chrome ext review confirmed)
Supermemory: server-side, $19/mo
Mem AI: pivoted after burning $40M
Markdown files: stale within days

Snapcommit is genuinely local + genuinely cross-tool.

---

**6/**
The paid part is LLM extraction.

You give Snapcommit a conversation chunk → it returns structured memories. Powered by GPT-5 Nano (cheapest with best structured-output quality), with Gemini Flash failover.

Your content is processed in-flight. NEVER stored on our servers.

---

**7/**
Pricing:

→ Hobby $9/mo — 200 extractions/month
→ Pro $29/mo — 2,000/month
→ Studio $129/mo — 10,000/month

Local memory is free open source forever. The paid tiers unlock the LLM extraction.

No free trial. Live demo on the homepage to try before subscribing.

---

**8/**
Try the demo: snapcommit.com

No signup. Paste any conversation. See exactly what extractions your AI would save automatically.

3 free demos per IP per day.

---

**9/**
Stack is built for solo-dev sustainability:

→ Open-source MCP server (MIT, TypeScript)
→ Supabase backend (Postgres + Edge Functions + Auth)
→ Dodo Payments (merchant of record, handles tax globally)
→ ~95% margin per subscription

Zero "we need to raise" pressure.

---

**10/**
Why I built this:

I'm a solo founder. I work across Claude Code + Cursor + ChatGPT daily, across 5 ongoing projects. The context-loss tax was killing me. I built the thing I needed.

If you've felt this pain — try the demo. If it helps, subscribe.

snapcommit.com

---

**11/** (optional — only if you want to commit)
What's next:

→ Smarter conflict detection across devices
→ Voice extraction (talk-to-Snapcommit)
→ Notion two-way sync
→ Public roadmap on GitHub

Solo dev, MIT license, here for the long run.

---

## Engagement playbook

- Reply to every quote-tweet for 24 hours
- Like every reply (signal of life)
- If someone has a real question, DM them
- Don't engage with negative replies that misunderstand — let supporters do that for you
- At hour 6: post a "thanks, here's the early traction" tweet with a screenshot of demo widget usage

## Boost candidates (DM ahead of launch day with a heads up)

- @swyx
- @theo (t3.gg)
- @pcousineau (sindresorhus is hard but maybe)
- Any AI/MCP-adjacent dev with 5K+ followers you have any rapport with
